import { Card, CardBody, CardTitle, Container, Row, Col } from "reactstrap";

const Header = () => {
  return (
    <>
      <div className="header bg-default pb-6 pt-5 pt-md-8">
        <Container fluid>
        </Container>
      </div>
    </>
  );
};

export default Header;
